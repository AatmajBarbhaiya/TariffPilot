# ─────────────────────────────────────────────────────────────────────────
# TariffPilot — all Python dependencies in one file.
#   pip install -r requirements.txt
#
# Versions are pinned to the set verified running on the AMD MI300X droplet
# (ROCm 7.2.4, Ubuntu 24.04) so the project is reproducible end-to-end.
# ─────────────────────────────────────────────────────────────────────────

# LLM client — OpenAI-compatible SDK. Talks to our self-hosted vLLM server
# (openai/gpt-oss-20b on the AMD MI300X) and, optionally, Fireworks as fallback.
openai==2.45.0

# Vector store + semantic search. Query-time embedding uses Chroma's built-in
# ONNX MiniLM, so NO torch is required just to run the app.
chromadb==1.5.9

# Backend HTTP API — the endpoint the demo UI and the judge call.
fastapi==0.139.0
uvicorn[standard]==0.51.0

# Front-end.
streamlit==1.59.1

# HTTP: UI→backend calls, plus WITS / USITC / UK-Tariff data ingestion.
requests==2.34.2

# ── Build / ingest only (optional) ─────────────────────────────────────────
# Needed ONLY to REBUILD the Chroma vector index from scratch
# (data_ingestion/ingest_taxonomy.py). The prebuilt index already ships in
# Database/, so the app runs without it. Pulls in torch (large) — uncomment
# only if you intend to re-embed the taxonomy.
# sentence-transformers
