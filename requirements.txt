# data layer (ingestion + Chroma)
requests
chromadb
sentence-transformers        # build/ingest-time embedder only; NOT needed to serve

# retrieval + serving
openai                        # OpenAI-compatible client for llama-server / Fireworks
